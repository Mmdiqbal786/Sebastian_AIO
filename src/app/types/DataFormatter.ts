// import { IEventTag } from "./EventTag";

// export type DataFormatter<T> = (data: T[]) => { label: string; value: string }[];

// export const formatTags = (data: IEventTag[]): { label: string; value: string }[] => {
//   return data.map((tag) => ({
//     label: tag.tag,
//     value: tag.id.toString(),
//   }));
// };